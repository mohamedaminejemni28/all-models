# all-models

Machine learning model comparison workspace for gait datasets.

This repository contains the ALL_MODELS work, including SVM/RBF, XGBoost, Random Forest, MLP, LightGBM, CatBoost, FT-Transformer, Autoencoder Classifier, backend, frontend, and documentation files.
